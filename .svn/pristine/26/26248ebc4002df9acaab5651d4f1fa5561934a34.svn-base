<template>
  <div class="dPContainer">
    <Datepicker v-model="date" :config="options"></Datepicker>
  </div>
</template>

<script >
import Datepicker from "vue-bootstrap-datetimepicker";
import "pc-bootstrap4-datetimepicker/build/css/bootstrap-datetimepicker.css";
import moment from "moment";
import $ from "jquery";
/* import * as moment from "moment"; */
/* $(".day disabled").hover(function() {
  alert("test");
}); */

export default {
  name: "Date",
  components: {
    Datepicker
  },
  props: {
    IsProgramaticOpen: {
      type: Boolean,
      default: false
    },
    PlaceHolder: {
      type: String,
      default: "Select Date"
    },
    DateFormat: {
      type: String,
      default: "MM/dd/yyyy"
    },
    SelectedDate: {
      type: String,
      default: Date(Date.now())
    }
  },
  mounted() {
    const d = $("#dPContainer");
    console.log(d.datetimepicker());
    $("#dPContainer").hover(() => {
      alert("test");
    });
  },
  data() {
    return {
      date: new Date(),
      options: {
        format: "DD/MM/YYYY",
        useCurrent: false,
        disabledDates: [moment("18/04/2019", "DD/MM/YYYY")]
      }
    };
  },
  methods: {
    openPicker() {
      this.$refs.programaticOpen.showCalendar();
      this.$refs.programaticOpen.$el.querySelector("input").focus();
    },

    onDateSelect(selecteddate) {
      this.$emit("onDateSelect", selecteddate);
    }
  }
};
</script>

<style lang="scss" scoped>
</style>
