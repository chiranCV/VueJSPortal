const Cookies = require("js-cookie");

// session manager to manage cooekies
class SessionManager {
  // set cookie(name: cookiie kye, value: vlaue, time: expiration time[munits]).
  static setCookie = (name, value, time = 1) => {
    // if (!this.chekcCookieExsitst(name)) {
    const timeindays = 0.00069 * time;
    Cookies.set(name, value, { expires: timeindays });
    // }
  };

  // chekc cookie exsist (name: cookiie kye).
  static chekcCookieExsitst = (name) => {
    console.logs("cookie ", this.getCookie(name));
    if (this.getCookie(name)) {
      return true;
    }
    return false;
  };

  // get cookie by key (name: cookiie kye).
  getCookie = name => Cookies.get(name);
}

export default SessionManager;
