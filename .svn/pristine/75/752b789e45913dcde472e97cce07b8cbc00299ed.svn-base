import { UPDATE_SANATEXT_SATE } from "../../mutation-types";

export default {
  // update sanatext state in vuex store.
  [UPDATE_SANATEXT_SATE]: (state, payload) => {
    const curentState = state;
    if (payload) {
      curentState.ProductList = payload;
    }
  }
};
